version: "3"
networks:
  backend:
services: 
  db:
    image: "mongo:latest"
    ports:
      - 27017:27017
    networks: 
    - backend
  cache:
    image: redis:alpine
    networks: 
    - backend
  backend: 
    build: 
      context: .
      dockerfile: Dockerfile
    depends_on:
      - db
      - cache
    environment:
      - DB=mongodb://db
      - REDIS=redis://cache
      - ENV=dev
    volumes:
      - ./:/raven
    ports: 
      - "3001:3001"
    networks:
      - backend
  frontend:
    build: ./client
    ports:
      - "3000:3000"
    volumes: 
      - ./client:/client
    depends_on: 
      - backend
